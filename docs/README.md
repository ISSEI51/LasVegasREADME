# Documentation assets

Placeholders referenced by the project README:

- `screenshot.png` — a generated README rendered on GitHub.
- `demo.gif` — the CLI running end to end in a terminal.

Both are intentionally absent from the repository; drop your own captures here.
